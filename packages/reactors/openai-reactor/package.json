{
  "name": "@ekairos/openai-reactor",
  "version": "1.22.0",
  "description": "Codex-only reactor for @ekairos/events",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "node -e \"require('fs').rmSync('dist', {recursive:true, force:true})\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:reactor": "vitest run src/tests/codex.reactor.test.ts --reporter=verbose",
    "test:reactor:instant": "vitest run src/tests/codex.reactor.instant.test.ts --reporter=verbose",
    "pretest:reactor:workflow": "pnpm --filter @ekairos/domain run build && pnpm --filter @ekairos/events run build",
    "test:reactor:workflow": "vitest run -c vitest.workflow.config.mts"
  },
  "peerDependencies": {
    "@ekairos/events": "workspace:*"
  },
  "devDependencies": {
    "@ekairos/domain": "workspace:*",
    "@ekairos/events": "workspace:*",
    "@ekairos/tsconfig": "workspace:*",
    "@instantdb/admin": "0.22.158",
    "@types/node": "^24.5.0",
    "@workflow/serde": "5.0.0-beta.0",
    "@workflow/vitest": "5.0.0-beta.1",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.2",
    "vitest": "^4.0.8",
    "workflow": "5.0.0-beta.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/e-kairos/ekairos.git"
  },
  "homepage": "https://github.com/e-kairos/ekairos#readme",
  "bugs": {
    "url": "https://github.com/e-kairos/ekairos/issues"
  },
  "dependencies": {
    "@ekairos/sandbox": "workspace:*"
  }
}
